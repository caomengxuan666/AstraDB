# ==============================================================================
# Memory Unit Tests
# ==============================================================================

if(ASTRADB_BUILD_TESTS)
  # TODO: Enable memory tests after fixing header paths
  # target_sources(astradb_tests PRIVATE
  #   arena_test.cpp
  # )
endif()