# ==============================================================================
# Core Unit Tests
# ==============================================================================

if(ASTRADB_BUILD_TESTS)
  # TODO: Enable executor tests after fixing Abseil dependencies
  # target_sources(astradb_tests PRIVATE
  #   executor_test.cpp
  # )
endif()